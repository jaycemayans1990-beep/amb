-- 0. Rayfield Library Setup & Notification Override
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- สั่งปิดการแสดง Notification และคำเตือนทั้งหมด
Rayfield.Notify = function() end 

-- 1. Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

-- 2. Global States
local State = {
    Aimbot = false,
    TeamCheck = true,
    WallCheck = false,
    Prediction = false,
    Smoothness = 0.3,
    FOV = 150,
    ShowFOV = false,
    ESP = false
}

-- 3. Dynamic Camera Helper
local function getCamera()
    return Workspace.CurrentCamera
end

-- 4. FOV Circle Drawing Engine
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Filled = false
FOVCircle.Transparency = 0.7
FOVCircle.Visible = false

-- 5. Target Validation Check
local function isValidEnemy(player)
    if player == LocalPlayer or not player.Character then return false end
    
    local humanoid = player.Character:FindFirstChild("Humanoid")  
    local rootPart = player.Character:FindFirstChild("HumanoidRootPart")  
      
    if not humanoid or not rootPart or humanoid.Health <= 0 then   
        return false   
    end  
    
    if player.Character:FindFirstChildOfClass("ForceField") then  
        return false  
    end  
    
    if State.TeamCheck then  
        if player.Team and LocalPlayer.Team and player.Team == LocalPlayer.Team then  
            return false  
        end  
    end  
    
    return true
end

-- 6. Wall Check Function
local function isVisible(targetPart)
    if not State.WallCheck then return true end
    
    local camera = getCamera()  
    local origin = camera.CFrame.Position  
    local destination = targetPart.Position  
    local direction = destination - origin  
      
    local raycastParams = RaycastParams.new()  
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude  
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, camera}  
    raycastParams.IgnoreWater = true  
      
    local result = Workspace:Raycast(origin, direction, raycastParams)  
      
    if result then  
        return result.Instance:IsDescendantOf(targetPart.Parent)  
    end  
      
    return false
end

-- 7. Target Acquisition
local function getClosestTarget()
    local closestPlayer = nil
    local shortestDist = State.FOV
    local camera = getCamera()
    local screenCenter = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    
    for _, player in ipairs(Players:GetPlayers()) do  
        if isValidEnemy(player) then  
            local targetPart = player.Character:FindFirstChild("Head") or player.Character:FindFirstChild("HumanoidRootPart")  
            if targetPart and isVisible(targetPart) then  
                local screenPoint, onScreen = camera:WorldToViewportPoint(targetPart.Position)  
                  
                if onScreen then  
                    local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - screenCenter).Magnitude  
                    if dist < shortestDist then  
                        shortestDist = dist  
                        closestPlayer = player  
                    end  
                end  
            end  
        end  
    end  
    return closestPlayer
end

-- 8. Prediction Engine
local function getPredictedPosition(target)
    if not target or not target.Character then return nil end
    
    local targetPart = target.Character:FindFirstChild("Head") or target.Character:FindFirstChild("HumanoidRootPart")  
    if not targetPart then return nil end  
      
    local targetPos = targetPart.Position  
    if State.Prediction then  
        local rootPart = target.Character:FindFirstChild("HumanoidRootPart")  
        if rootPart then  
            local velocity = rootPart.AssemblyLinearVelocity  
            targetPos = targetPos + (velocity * 0.08)  
        end  
    end  
      
    return targetPos
end

-- 9. Smooth Aimbot Process
local function processAimbot()
    if not State.Aimbot then return end
    
    local target = getClosestTarget()  
    if target then  
        local targetPos = getPredictedPosition(target)  
        if targetPos then  
            local camera = getCamera()  
            local targetCFrame = CFrame.lookAt(camera.CFrame.Position, targetPos)  
            camera.CFrame = camera.CFrame:Lerp(targetCFrame, State.Smoothness)  
        end  
    end
end

-- 10. ESP Management
local function clearESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player.Character then
            local highlight = player.Character:FindFirstChild("ESP_Highlight")
            if highlight then
                highlight:Destroy()
            end
        end
    end
end

local function processESP()
    if not State.ESP then
        clearESP()
        return
    end
    
    for _, player in ipairs(Players:GetPlayers()) do  
        if isValidEnemy(player) then  
            local highlight = player.Character:FindFirstChild("ESP_Highlight")  
            if not highlight then  
                highlight = Instance.new("Highlight")  
                highlight.Name = "ESP_Highlight"  
                highlight.FillColor = Color3.fromRGB(180, 0, 255)
                highlight.OutlineColor = Color3.fromRGB(255, 255, 0)
                highlight.FillTransparency = 0.15
                highlight.OutlineTransparency = 0
                highlight.Parent = player.Character
            end  
        else  
            if player.Character then  
                local highlight = player.Character:FindFirstChild("ESP_Highlight")  
                if highlight then highlight:Destroy() end  
            end  
        end  
    end
end

-- 11. Main Loop Handling
RunService.RenderStepped:Connect(function()
    local camera = getCamera()
    
    FOVCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)  
    FOVCircle.Radius = State.FOV  
    FOVCircle.Visible = State.ShowFOV  
      
    processAimbot()  
    processESP()
end)

-- 12. Rayfield UI Configuration (No Notifications)
local Window = Rayfield:CreateWindow({
    Name = "ADVANCED AIMBOT SYSTEM (FIXED)",
    LoadingTitle = "",
    LoadingSubtitle = "",
    Theme = "Default",
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true,
    ConfigurationSaving = {
        Enabled = false
    }
})

-- Combat Tab
local CombatTab = Window:CreateTab("Combat", 4483362458)
CombatTab:CreateSection("Aimbot Controls")

CombatTab:CreateToggle({
    Name = "Enable Aimbot",
    CurrentValue = State.Aimbot,
    Flag = "AimbotToggle",
    Callback = function(Value)
        State.Aimbot = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Show FOV Circle (แสดงวงกลม FOV)",
    CurrentValue = State.ShowFOV,
    Flag = "ShowFOVToggle",
    Callback = function(Value)
        State.ShowFOV = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Team Check (ปิดเมื่อเล่นเกมแบบไม่มีทีม/FFA)",
    CurrentValue = State.TeamCheck,
    Flag = "TeamCheckToggle",
    Callback = function(Value)
        State.TeamCheck = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Wall Check (ไม่ล็อคเป้าหลังกำแพง)",
    CurrentValue = State.WallCheck,
    Flag = "WallCheckToggle",
    Callback = function(Value)
        State.WallCheck = Value
    end,
})

CombatTab:CreateToggle({
    Name = "Prediction Engine",
    CurrentValue = State.Prediction,
    Flag = "PredictionToggle",
    Callback = function(Value)
        State.Prediction = Value
    end,
})

CombatTab:CreateSlider({
    Name = "Smoothness (ความลื่นไหล)",
    Range = {0.05, 1},
    Increment = 0.05,
    Suffix = "",
    CurrentValue = State.Smoothness,
    Flag = "SmoothnessSlider",
    Callback = function(Value)
        State.Smoothness = Value
    end,
})

CombatTab:CreateSlider({
    Name = "Aimbot FOV Range",
    Range = {50, 500},
    Increment = 10,
    Suffix = "px",
    CurrentValue = State.FOV,
    Flag = "FOVSlider",
    Callback = function(Value)
        State.FOV = Value
    end,
})

-- Visuals Tab
local VisualsTab = Window:CreateTab("Visuals", 4483362458)
VisualsTab:CreateSection("ESP Visuals")

VisualsTab:CreateToggle({
    Name = "ESP Wallhack",
    CurrentValue = State.ESP,
    Flag = "ESPToggle",
    Callback = function(Value)
        State.ESP = Value
    end,
})
